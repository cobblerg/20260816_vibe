# Claude & Claude Code 시작 가이드
## 바이브 코딩 워크숍 참여자용

---

# PART 1. 연수 전에 해주세요 (20분)

> 계정(Pro)과 GitHub는 준비되었습니다. **설치와 로그인만** 미리 끝내주세요.
> 당일에 설치하면 오전을 통째로 날립니다. 막히면 혼자 붙들지 말고 담당자에게 연락 주세요.

## STEP 1. 설치

공식 안내: **https://code.claude.com/docs/en/setup**

### 내 컴퓨터가 되는지 먼저
macOS 13.0 이상 / Windows 10(1809) 이상 / Ubuntu 20.04 이상, RAM 4GB 이상

### 터미널 여는 법
| OS | 방법 |
|---|---|
| **Windows** | 시작 메뉴 → "터미널" 또는 "PowerShell" 검색 |
| **macOS** | `Cmd + Space` → "터미널" 입력 |

### 설치 명령 (터미널에 붙여넣고 Enter)

**Windows (PowerShell)**
```powershell
irm https://claude.ai/install.ps1 | iex
```

**macOS / Linux**
```bash
curl -fsSL https://claude.ai/install.sh | bash
```

**Windows 추가 권장**: [Git for Windows](https://git-scm.com/downloads/win) — 설치 중 선택지는 전부 기본값으로 두고 "다음"만 누르면 됩니다.

## STEP 2. 확인 & 로그인

**터미널을 완전히 닫았다가 새로 연 뒤:**

```bash
claude --version
```
→ `2.x.x (Claude Code)` 같은 버전 번호가 나오면 성공

```bash
claude
```
→ 브라우저가 열리며 로그인을 요청합니다. **Pro 계정으로** 로그인해 주세요.
→ 종료는 `Ctrl + D` 또는 `exit`

**문제가 있으면**
```bash
claude doctor
```
→ 결과를 캡처해서 담당자에게 보내주세요.

## STEP 3. 백업 도구 설치 (Codex CLI) ★ 추가 결제 없음

Claude 사용량 한도에 걸렸을 때를 대비한 백업입니다.
**ChatGPT 무료 계정으로도 Codex CLI를 쓸 수 있어서, 돈이 더 들지 않습니다.**

```bash
npm install -g @openai/codex        # 또는 macOS: brew install --cask codex
codex --version
codex                                # ChatGPT 계정으로 로그인
```

미리 깔아두면 당일에 **명령 하나로 전환**됩니다. 당일에 설치를 시작하면 실습 시간이 통째로 날아갑니다.

## STEP 4. 제출

- [ ] `claude --version` 실행 결과 **화면 캡처** → 사전 설문에 첨부
- [ ] `codex --version` 실행 결과 **화면 캡처** → 사전 설문에 첨부
- [ ] GitHub 로그인 확인
- [ ] 사전 진단 설문 응답

---

## ⚠️ 연수 당일 아침 부탁

**연수 당일 오전에는 개인 작업으로 Claude를 쓰지 말아 주세요.**
사용량이 웹 채팅과 Claude Code에서 **하나로 합산**되기 때문에, 아침에 많이 쓰면 오후 실습이 막힙니다. (자세한 내용은 PART 2 참고)

---

## 터미널이 도저히 안 되면 — 트랙 B

터미널 자체가 부담이시면 **Claude 데스크톱 앱**으로 터미널 없이 Claude Code를 쓸 수 있습니다.
안내: https://code.claude.com/docs/en/setup 의 *Desktop app* 항목
당일에도 언제든 갈아탈 수 있습니다. **오늘의 목표는 터미널 정복이 아닙니다.**

## 자주 나는 오류

| 증상 | 원인 | 해결 |
|---|---|---|
| `command not found: claude` | 경로 미등록 | **터미널 껐다 다시 열기** → `claude doctor` |
| `'irm' is not recognized...` | PowerShell이 아니라 CMD | 프롬프트가 `PS C:\`로 시작해야 PowerShell |
| `The token '&&' is not a valid...` | CMD 명령을 PowerShell에 입력 | 위 표의 PowerShell용 명령 사용 |
| 로그인 브라우저가 안 열림 | 브라우저 차단 | 터미널의 URL을 직접 복사해 붙여넣기 |

---
---

# PART 2. Claude 기본 사용법

## Claude는 어떤 도구인가

대화형 AI 어시스턴트입니다. 글쓰기·분석·자료 정리·코딩·문서 읽기를 합니다.
우리에게 중요한 특성은 두 가지입니다.

- **맥락을 기억한다** — 단, **한 대화 안에서만**
- **비결정적이다** — 같은 질문에 매번 조금씩 다른 답

이 두 가지가 오늘 하루 내내 따라다닙니다.

---

## 화면 둘러보기 (claude.ai)

| 기능 | 무엇 | 언제 |
|---|---|---|
| **새 대화** | 맥락 초기화 | **주제가 바뀔 때마다** |
| **파일 첨부** | PDF·이미지·문서 업로드 | 공문 요약, 자료 분석 |
| **아티팩트** | 결과물을 별도 창에 생성 | 문서·코드·웹페이지 |
| **프로젝트** | 자주 쓰는 자료를 상주시킴 | 교육과정 문서, 학교 양식 |
| **스타일** | 응답 톤·형식 고정 | 공문체, 학생용 쉬운 말 |
| **설정 → Usage** | 사용량 확인 | **오늘 자주 볼 것** |

---

## 좋은 지시의 네 가지 요소

| 요소 | 질문 | 예 |
|---|---|---|
| **맥락** | 나는 누구, 이건 왜? | "중2 정보 수업용" |
| **과제** | 정확히 무엇을? | "형성평가 문항 5개" |
| **제약** | 무엇을 지켜야? | "객관식, 코드 작성 문항 제외" |
| **형식** | 어떻게 출력? | "표로, 정답과 해설 포함" |

```
✗ "평가 문제 좀 만들어줘"

✓ "중학교 2학년 정보 수업의 '순차·선택·반복 구조' 단원 형성평가를 만든다.
   4지선다 5문항, 코드를 직접 작성하는 문항은 제외.
   표로 출력하고 정답과 해설을 함께 넣어줘."
```

> 왼쪽 문장이 못 쓰는 이유는 Claude가 답을 못 해서가 아니라 **매번 답이 달라져서**입니다.
> 오후에 배울 PRD는 이 4요소를 **문서로 고정**하는 것입니다.

---

## 대화 맥락 관리 — 가장 많이 놓치는 것

- Claude는 **한 대화 안의 내용**만 참고합니다. 새 대화는 백지입니다.
- 대화가 길어질수록 앞 내용이 흐려지고, **매 응답의 비용이 커집니다.**
- 원칙: **한 대화 = 한 주제.** 주제가 바뀌면 새 대화.

---

## ⚠️ 사용량 관리 — 오늘 꼭 알아야 할 것

> **claude.ai 웹, 데스크톱 앱, Claude Code의 사용량은 모두 같은 풀에서 나갑니다.**
> 5시간 단위 세션 한도 + 주간 한도가 함께 적용됩니다.
> 대화 길이, 복잡도, 사용 기능, 모델에 따라 소모 속도가 달라집니다.

**아끼는 법**
- 주제가 바뀌면 **새 대화** (긴 대화는 매 턴 비용 증가)
- 자주 쓰는 자료는 **프로젝트에 저장**해 재업로드 줄이기
- 단순한 작업은 **가벼운 모델**로 (`/model`)
- Claude Code에서 **`/status`**로 남은 양 확인

**한도에 걸리면**
- 5시간 뒤 리셋됩니다. 정확한 리셋 시각은 **설정 → Usage**에서 확인할 수 있습니다.
- **Codex CLI를 미리 깔아두셨다면 30초면 됩니다** (아래 STEP 3 참고).
- 그 외에는 손을 드세요. 강사가 상황에 따라 안내합니다.
  1. **짝과 화면 공유로 페어 작업**
  2. **AI가 필요 없는 작업으로 전환** — PRD 다듬기, 검증 체크리스트, 화면 스케치
  3. **API 키 임시 대여** — 같은 Claude Code를 그대로 사용 (강사 제공)

> ⚠️ **"API 크레딧으로 계속하시겠습니까?" 안내가 뜨면 본인 결제입니다.**
> 강사 안내를 받지 않았다면 **"아니오"**를 선택하세요.
> 아예 안 뜨게 하려면: `claude logout` → `claude login` → **Pro 계정만** 로그인

### Codex로 전환하기 (30초)

```bash
cp CLAUDE.md AGENTS.md      # Windows: copy CLAUDE.md AGENTS.md
codex
```
첫 지시:
```
AGENTS.md와 PRD.md를 읽고 현재 코드 상태를 파악해줘.
그 다음 FR-0X부터 이어서 작업한다.
```

> **`PRD.md`는 손댈 필요가 없습니다.** 그냥 마크다운 텍스트니까요.
> 파일 이름 하나(`CLAUDE.md` → `AGENTS.md`)만 바뀝니다. `AGENTS.md`는 여러 AI 코딩 도구가 공용으로 쓰는 표준 파일명입니다.
> **결과가 Claude와 어떻게 달랐는지 메모해 두세요.** 2일차 발표에 쓸 수 있습니다.

---

## AI의 한계 — 검증이 필요한 이유

| 한계 | 증상 | 대응 |
|---|---|---|
| **환각** | 그럴듯한 거짓을 자신 있게 말함 | 사실·수치·출처는 **직접 확인** |
| **최신 정보** | 최근 사건·문서를 모를 수 있음 | 원문 첨부 또는 검색 활용 |
| **비결정성** | 같은 요청에 다른 결과 | **명세를 구체적으로** |
| **맥락 부재** | 우리 학교 사정을 모름 | 배경을 명시적으로 제공 |

> **교육과정 성취기준, 법령, 통계 수치는 반드시 원문을 확인하세요.**
> 그럴듯한 성취기준 번호를 만들어낼 수 있습니다.
> — 이것이 오늘의 원칙 후반부, **"교사는 결정적으로 검증한다"**의 출발점입니다.

---
---

# PART 3. Claude Code 치트시트

## 터미널 명령 — 오늘 쓸 건 이게 전부

| 명령 | 뜻 |
|---|---|
| `pwd` | 나 지금 어느 폴더? |
| `ls` (Win: `dir`) | 여기 뭐 있어? |
| `cd 폴더명` | 그 폴더로 들어가기 |
| `cd ..` | 한 칸 위로 |
| `mkdir 폴더명` | 폴더 만들기 |
| `claude` | Claude Code 시작 |
| `exit` / `Ctrl+D` | 나가기 |
| `open index.html` (Win: `start index.html`) | 브라우저로 열기 |

**시작 루틴**
```bash
cd Desktop
mkdir my-tool
cd my-tool
claude
```

---

## 세 가지 모드 — `Shift + Tab`으로 순환

| 모드 | 동작 | 언제 |
|---|---|---|
| **일반** | 변경마다 승인 요청 | 기본 |
| **플랜(Plan)** | **코드 안 쓰고 계획만 제시** | 새 기능 시작 전 ★권장 |
| **자동 수락** | 승인 없이 진행 | ⚠️ 오늘은 사용 금지 |

> 화면 하단의 모드 표시를 수시로 확인하세요. 긴 대화에서 모드가 풀릴 수 있습니다.

---

## 슬래시 명령

| 명령 | 용도 |
|---|---|
| **`/help`** | **전체 명령 목록 — 이것만 외우면 됩니다** |
| **`/status`** | **남은 사용량 확인** (오늘 자주) |
| `/init` | 프로젝트를 분석해 `CLAUDE.md` 생성 |
| `/clear` | 대화 맥락 초기화 (주제가 바뀔 때) |
| `/compact` | 긴 대화를 요약해 압축 |
| `/model` | 모델 변경 |
| `# 내용` | 그 내용을 메모리에 추가 |

## 키

| 키 | 동작 |
|---|---|
| `Shift + Tab` | 모드 전환 |
| `Esc` | 진행 중인 작업 중단 |
| `Esc` 두 번 | 되돌리기 메뉴 |
| `↑` / `↓` | 이전 입력 불러오기 |
| `Ctrl + D` | 종료 |

---

## 승인 화면 읽는 법

```
  index.html
- <div class="old">     ← 빨강: 지워지는 것 ★먼저 보세요
+ <div class="new">     ← 초록: 추가되는 것
```

**체크 3가지**
1. 어떤 **파일**을 건드리는가
2. **무엇을 지우는가** (빨강)
3. 내가 요청하지 **않은 것**이 섞여 있지 않은가

> 습관적으로 승인 누르지 않기. **읽지 않은 변경은 승인하지 않는다** — 오늘의 1번 수칙.

---

## 안전망: Git

```bash
git init                      # 이 폴더 관리 시작 (처음 한 번만)
git add .                     # 변경사항 담기
git commit -m "설명"           # 저장 지점 만들기
git log --oneline             # 저장 지점 목록
```

> Claude Code에게 **"지금 상태를 커밋해줘"**라고 시켜도 됩니다. 다만 무엇을 하는지는 알고 시키세요.

**커밋 타이밍**
- ✅ 기능 하나가 **동작을 확인한 직후**
- ✅ 큰 변경을 **시도하기 직전**
- ❌ "일단 다 만들고 한 번에" ← 되돌릴 수 없습니다

**망가졌을 때**

| 상황 | 대응 |
|---|---|
| 방금 승인한 게 잘못됨 | "방금 변경 되돌려줘" |
| 대화가 꼬임 | `Esc` 두 번 → 되돌리기 |
| 파일이 엉망 | `git log --oneline` → 이전 커밋으로 복원 요청 |
| 뭐가 문제인지 모름 | `/clear` 후 **커밋 시점부터 다시** |

> 같은 버그를 **세 번** 고쳐도 안 되면, 고치지 말고 **되돌아가서 다시 시작**하는 게 빠릅니다.

---

## 프로젝트 폴더 구조 (오늘 만들 것)

```
my-tool/
├── CLAUDE.md      ← 프로젝트 규칙 (변하지 않는 것) — 오전
├── PRD.md         ← 이번에 만들 것의 명세 — 오후
├── README.md      ← 한 줄 설명 (배포 시)
└── index.html     ← 실제 앱
```

---

## `CLAUDE.md` 예시 — 그대로 복사해 쓰세요

```markdown
# 프로젝트: [도구 이름]

## 이 프로젝트의 규칙
- 단일 HTML 파일(index.html)로만 구성한다. 빌드 도구를 쓰지 않는다.
- 외부 라이브러리·CDN을 추가하지 않는다.
- 모든 UI 문구는 한국어로 쓴다.
- 학생 개인정보(학번·연락처·사진)를 다루는 코드를 만들지 않는다.
- 데이터는 localStorage에만 저장하고 외부로 전송하지 않는다.
- 본문 글자 크기는 16px 이상으로 한다.

## 작업 방식
- 새 기능은 구현 전에 계획을 먼저 제시한다.
- 요청에 없는 기능을 임의로 추가하지 않는다.
- 한 번에 한 가지 변경만 한다.

## 명세 규칙  (오후에 PRD.md를 만든 뒤 추가)
- PRD.md가 이 프로젝트의 유일한 명세다.
- PRD.md의 "Out of Scope"와 "비목표"에 적힌 것은 어떤 이유로도 구현하지 않는다.
- 구현 전, 어떤 FR-ID를 구현하는지 먼저 밝힌다.
- 구현 후, 해당 FR의 인수 조건을 하나씩 대조해 통과 여부를 보고한다.
- 내 요청이 PRD와 충돌하면, 구현하지 말고 충돌 사실을 먼저 알린다.
- 명세가 모호하면 추측하지 말고 질문한다.
```

---

## 오늘의 안전 수칙 3가지

1. **읽지 않은 변경은 승인하지 않는다**
2. **커밋은 자주** — 되돌아갈 지점 없이 실험하지 않는다
3. **실제 학생 개인정보는 넣지 않는다** — 오늘은 가상 명단만

> 3번 보충: **Git 커밋 기록은 파일을 지워도 남습니다.** 처음부터 넣지 마세요.
> 오늘 만드는 것은 수업 도구의 **시제품**입니다. 실제 투입 전에는 학교 정보보호 절차를 거치세요.

---

## 막혔을 때 순서

1. 오류 메시지를 **그대로 복사**해서 Claude Code에 붙여넣기
2. `claude doctor` 실행
3. `/clear` 후 다시 시도
4. 마지막 커밋으로 되돌리기
5. **손 들기** — 5분 이상 혼자 붙들지 마세요
