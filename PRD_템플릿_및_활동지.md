# 참여자 배포 자료
## 요구사항 분석과 PRD 작성법 — 활동지 · 템플릿 · 프롬프트

이름: ______________  만들 도구: ______________________________

---

## 활동지 1 — 문제 진술 캔버스 (5분)

```
① 누가        [                                        ]는

② 언제        [                                        ] 상황에서

③ 무슨 문제   [                                        ]를 겪는다.

④ 지금은      현재는 [                    ]으로 해결하지만

⑤ 한계        [                                        ]가 있다.

⑥ 그래서      따라서 [                                 ]이 필요하다.

⑦ 성공 신호   성공했다면 [                             ]로 확인할 수 있다.
              (반드시 숫자 또는 관찰 가능한 사실로)
```

**⑦ 작성 예시**
- ✗ "수업이 편해진다"
- ✓ "발표 순서 정하는 데 쓰던 3분이 20초 이내로 줄어든다"
- ✓ "학기당 12회 사용해도 동일 학생이 1번을 두 번 뽑지 않는다"

---

## 활동지 2 — 사용자 스토리 & MoSCoW (15분)

### 2-1. 사용자 스토리 (5개)

```
S1  [ 역할 ]로서, [ 행동 ]하고 싶다. [ 이유 ] 때문에.
    →

S2  →

S3  →

S4  →

S5  →
```

**자가 점검**: 각 문장에 **동사**와 **"~때문에"**가 들어 있는가? 없으면 다시.

### 2-2. MoSCoW 배분

| 등급 | 항목 | 개수 제한 |
|---|---|---|
| **Must** (없으면 성립 불가) | | **최대 4개** |
| **Should** (있으면 실사용 품질) | | 2~3개 |
| **Could** (시간 남으면) | | 자유 |
| **Won't** (이번엔 안 함 — 명시!) | | **최소 3개** |

**Won't 작성 힌트**: 로그인/회원가입 · 서버 DB · 학생 개인정보 저장 · 다국어 · 관리자 페이지 · 실시간 동기화 · 앱 설치 · 결제 · 통계 대시보드 · 파일 업로드

> ⚠️ Must가 5개 이상이면 2개를 Should로 강등하세요. **해커톤은 하루입니다.**

---

## 활동지 3 — 흐린 문장 → 결정적 문장 (5분)

아래 왼쪽 문장을 AI가 오해할 수 없게 고쳐 쓰세요.

| # | 흐린 문장 | 결정적인 문장으로 |
|---|---|---|
| 1 | 화면이 깔끔했으면 좋겠다 | |
| 2 | 학생 정보를 안전하게 다룬다 | |
| 3 | 결과를 저장할 수 있다 | |
| 4 | 오류가 나지 않게 처리한다 | |
| 5 | 모바일에서도 잘 보인다 | |
| 6 | 사용하기 쉬워야 한다 | |

**모범 답안 (진행 후 공개)**

| # | 결정적인 문장 |
|---|---|
| 1 | 강조색 1개(#1B4B8F) + 회색조만 사용, 그림자·애니메이션 없음, 본문 16px 이상 |
| 2 | 모든 데이터는 브라우저 localStorage에만 저장, 외부 서버 전송 없음, 학번·연락처 필드 없음 |
| 3 | 최근 결과 1건을 localStorage에 보관, 새로고침 시 자동 복원, [초기화] 버튼으로 삭제 |
| 4 | 입력이 비면 버튼 비활성 + "명단을 입력하세요" 표시, 앞뒤 공백·빈 줄 자동 제거 |
| 5 | 360px 폭 기준 가로 스크롤 없음, 버튼 터치 영역 44px 이상 |
| 6 | 첫 화면부터 결과까지 조작 3회 이내, 사용 설명 없이 조작 가능 |

---

## PRD 템플릿 (9개 섹션)

> **사용법**: 아래를 복사해 `PRD.md`로 저장하고, 프로젝트 루트에 두세요.
> 해커톤 2일차에는 이 파일이 AI의 **유일한 명세**가 됩니다.

```markdown
# PRD: [도구 이름]

작성자: [이름] / 작성일: [YYYY-MM-DD] / 버전: v0.1

## 1. 한 줄 요약
[누가] [무엇]을 [어떻게] 할 수 있게 하는 [웹앱/도구].

## 2. 문제 정의와 성공 기준
**문제**: (활동지 1의 ①~⑥)

**성공 기준** (측정 가능하게)
- [ ] 
- [ ] 

## 3. 사용자와 핵심 시나리오
**주 사용자**: 
**사용 환경**: (교실 데스크톱 / 교사 노트북 / 학생 태블릿 / 스마트폰 …)
**핵심 시나리오** (3~5단계로)
1. 
2. 
3. 

## 4. 범위

### In Scope (이번에 만든다)
- 

### Out of Scope (이번에 만들지 않는다) ← 반드시 채울 것
- 
- 
- 

## 5. 기능 요구사항

| ID | 요구사항 | 등급 | 인수 조건 |
|---|---|---|---|
| FR-01 | | Must | Given ... / When ... / Then ... |
| FR-02 | | Must | Given ... / When ... / Then ... |
| FR-03 | | Must | |
| FR-04 | | Should | |
| FR-05 | | Could | |

## 6. 데이터 모델
**다루는 데이터**: 
**저장 위치**: (localStorage / 없음 / CSV 내보내기만 …)
**개인정보 처리**: (수집 항목 / 보관 기간 / 외부 전송 여부)

```
예시:
Student { name: string }              // 이름만, 학번·연락처 없음
Result  { order: number, name: string, createdAt: ISO8601 }
저장: localStorage 키 "presentation-order-v1", 최근 1건만
```

## 7. 화면 및 UX 명세
**화면 수**: 
**화면별 구성 요소**:
- 화면 1: 
- 화면 2: 

**디자인 제약**
- 색상: 
- 폰트/크기: 
- 반응형: 
- 접근성: 

## 8. 기술 제약
- 기술 스택: 
- 배포 방식: (GitHub Pages / 단일 HTML 파일 / …)
- 외부 의존성: (없음 / CDN 목록)
- 성능: 
- 브라우저: 

## 9. 비목표(Non-goals)와 리스크
**비목표** (하지 않기로 명시적으로 결정한 것)
- 
- 

**리스크와 대응**
| 리스크 | 대응 |
|---|---|
| | |
```

---

## 검증 체크리스트 양식

> **인수 조건을 그대로 옮겨 적고, 통과/실패로만 판정합니다. "대충 되는 듯"은 없습니다.**

```
[ FR-01 : ______________________ ]
  □ 정상 케이스:                             통과 / 실패
  □ 경계값 (0개):                             통과 / 실패
  □ 경계값 (1개):                             통과 / 실패
  □ 경계값 (최대):                            통과 / 실패
  □ 이상 입력 (공백·특수문자·중복):            통과 / 실패
  □ 상태 (새로고침·뒤로가기):                  통과 / 실패
  □ 화면 (모바일 360px):                      통과 / 실패

  실패 항목 → 수정 요청 문구:
  ______________________________________________
```

**경계값 점검 목록**: 0개 · 1개 · 최대 · 초과 · 빈 문자열 · 공백만 · 특수문자 · 이모지 · 중복 항목 · 매우 긴 이름 · 새로고침 · 뒤로가기 · 모바일 세로 · 느린 네트워크

---

## Claude Code 프롬프트 모음

### ① 요구사항 인터뷰용 (PRD 작성 시작 시) — **플랜 모드에서 실행**

> `Shift + Tab`으로 플랜 모드에 들어간 뒤 입력하세요. 코드를 쓰지 않고 대화만 합니다.

```
나는 [ 도구 ]를 만들려는 중학교 정보 교사다.
지금 바로 코드를 쓰지 마라.
대신 PRD를 완성하기 위해 나에게 질문하라.

- 한 번에 3개씩, 객관식 또는 예/아니오로 답할 수 있게 물어라
- 내가 미처 결정하지 못했을 지점을 우선 질문하라
- 특히 데이터 저장 방식, 개인정보 처리, 경계 상황 처리를 빠뜨리지 마라
- 충분해지면 PRD.md 파일로 아래 9개 섹션 초안을 작성하라
  (1 한 줄 요약 / 2 문제·성공기준 / 3 사용자·시나리오 / 4 범위 In·Out /
   5 기능요구사항+인수조건 / 6 데이터모델 / 7 화면·UX / 8 기술제약 / 9 비목표·리스크)
```

> ⚠️ AI의 질문에 **"알아서 해줘"라고 답하지 마세요.** 그 순간 백지 위임장으로 되돌아갑니다.
> 생성은 AI가, **결정은 내가.**

### ② 상주 컨텍스트용 — `CLAUDE.md`에 **추가로 적어두는** 내용

> 프롬프트로 매번 입력하지 말고, 프로젝트 폴더의 `CLAUDE.md` 파일에 넣으세요.
> 대화는 사라지지만 파일은 남습니다.

```markdown
## 명세 규칙
- PRD.md가 이 프로젝트의 유일한 명세다.
- PRD.md에 없는 기능은 임의로 추가하지 않는다.
  특히 "Out of Scope"와 "비목표"에 적힌 것은 어떤 이유로도 구현하지 않는다.
- 구현 전, 어떤 FR-ID를 구현하는지 먼저 밝힌다.
- 구현 후, 해당 FR의 인수 조건을 하나씩 대조해 통과 여부를 보고한다.
- 내 요청이 PRD와 충돌하면, 구현하지 말고 충돌 사실을 먼저 알린다.
- 명세가 모호하면 추측하지 말고 질문한다.
- 한 번에 한 가지 변경만 한다.
```

### ②-2 구현 사이클 (Must 하나마다 반복)
```
1. Shift+Tab → 플랜 모드
2. "FR-0X를 구현할 계획을 세워줘"
3. 계획을 읽고 → 토를 달고 → 수정 → 승인
4. 구현 (변경 내용의 빨간 줄부터 확인하고 승인)
5. 브라우저에서 직접 확인
6. 통과하면 즉시 커밋: "지금 상태를 'FR-0X 완료'로 커밋해줘"
```

### ③ 검증 요청용 (구현 후)
```
방금 구현한 FR-0X에 대해 아래를 수행하라.

1. PRD.md의 인수 조건을 그대로 인용하라
2. 각 조건이 코드의 어느 부분으로 충족되는지 지목하라
3. 아래 경계값에서 실제로 어떻게 동작하는지 코드 기준으로 설명하라
   (0개 / 1개 / 최대 / 빈 문자열 / 공백만 / 중복 / 특수문자 / 새로고침)
4. 충족하지 못하는 조건이 있으면 "미충족"이라고 명확히 말하라.
   충족한 것처럼 서술하지 마라.
```

### ④ 명세 갱신용 (요구가 바뀌었을 때)
```
요구사항이 바뀌었다. 코드를 먼저 고치지 마라.
아래 변경을 PRD.md에 먼저 반영하고, 변경된 부분(추가/수정/삭제 FR-ID)을 보여달라.
내가 확인한 뒤에 구현을 시작한다.

변경 내용: [ ... ]
```

---

## PRD 자가 점검표 — 해커톤 입장 체크리스트

> 2일차 시작 전, 모든 항목에 체크되어야 합니다.

**필수**
- [ ] 한 줄 요약을 **입 밖으로 소리 내어** 말할 수 있다
- [ ] 성공 기준에 **숫자 또는 관찰 가능한 사실**이 있다
- [ ] Must가 **4개 이하**다
- [ ] Out of Scope에 **3개 이상** 적혀 있다
- [ ] 모든 Must에 **인수 조건(Given-When-Then)**이 있다
- [ ] 인수 조건 중 **판정이 애매한 것이 하나도 없다**
- [ ] **개인정보 처리** 방침이 한 줄이라도 적혀 있다
- [ ] 배포 방식이 정해져 있다

**권장**
- [ ] 각 요구사항에 **FR-ID**가 붙어 있다
- [ ] 경계값 목록을 미리 적어두었다
- [ ] 화면 스케치(손그림 가능)가 있다
- [ ] "이걸 학생 활동지로 줄인다면?" 메모가 한 줄 있다

**환경 (Claude Code)**
- [ ] `claude --version`이 정상 동작한다
- [ ] 프로젝트 폴더에 **`PRD.md`**가 저장되어 있다
- [ ] 프로젝트 폴더에 **`CLAUDE.md`**가 있고, "명세 규칙" 항목이 들어 있다
- [ ] `git init` + 첫 커밋을 마쳤다
- [ ] GitHub 계정이 있고 로그인된다 (배포용)

---

### 기억할 한 문장

> **AI는 비결정적으로 생성하고, 교사는 결정적으로 검증한다.**
> 명세는 앵커, 인수 조건은 게이트.
